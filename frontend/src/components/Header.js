const headerStyle = {
  textAlign: "center",
  padding: "2%",
};

const Header = () => {
  return (
    <div style={headerStyle}>
      <h1 style={{ margin: "0" }}>Tim's Record Collection</h1>
    </div>
  );
};

export default Header;
